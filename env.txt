# Docker image name
export GENERO_DOCKER_IMAGE=

##### Apache file where passwords will be stored default will be apache-auth
export APACHE_AUTH_FILE=

##### gasadmin user password for apache default will be gasadmin
export GASADMIN_PASSWD=

# Apache port seen by the host default will be 8080
export HOST_APACHE_PORT=
##### Provide ROOT_URL_PREFIX base on HOST_APACHE_PORT
export ROOT_URL_PREFIX=http://localhost:${HOST_APACHE_PORT}/gas

##### FGLGWS package for a linux
export FGLGWS_PACKAGE=

##### GAS package
export GAS_PACKAGE=

